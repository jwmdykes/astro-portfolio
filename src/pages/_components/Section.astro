---
interface Props {
    class?: string
}

const { class: className } = Astro.props
---

<section
    class:list={[
        'border-3 rounded-xl mx-3 min-h-[max(500px,calc(100svh-72px-24px))] bg-background relative',
        className,
    ]}
>
    <slot />
</section>
