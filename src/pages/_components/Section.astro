---
interface Props {
    class?: string
}

const { class: className } = Astro.props
---

<section
    class:list={[
        'rounded-xl mx-3 min-h-[max(500px,calc(100svh-72px-24px))] bg-neutral-100 relative text-neutral-800',
        className,
    ]}
>
    <slot />
</section>
